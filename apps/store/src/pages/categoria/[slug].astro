---
import Layout from '../../layouts/Layout.astro';
import TopBar from '../../components/layout/TopBar.astro';
import Navbar from '../../components/layout/Navbar.astro';
import CategoryMenu from '../../components/layout/CategoryMenu.astro';
import Footer from '../../components/layout/Footer.astro';
import ProductCard from '../../components/features/products/ProductCard.astro';
import { productsApi } from '../../lib/api';

// Mapeo de categorías
const categories = {
  'frutas-verduras': {
    name: 'Frutas y Verduras',
    description: 'Encuentra la mejor selección de frutas y verduras frescas',
    image: '/images/categories/frutas-verduras.jpg',
    subcategories: ['Frutas', 'Verduras', 'Hierbas y Especias', 'Frutos Secos', 'Orgánicos']
  },
  'carnes-pescados': {
    name: 'Carnes y Pescados',
    description: 'Productos de primera calidad para tu negocio',
    image: '/images/categories/carnes.jpg',
    subcategories: ['Carnes Rojas', 'Pollo', 'Pescados', 'Mariscos', 'Embutidos']
  },
  'lacteos': {
    name: 'Lácteos',
    description: 'Lácteos frescos y de calidad',
    image: '/images/categories/lacteos.jpg',
    subcategories: ['Leche', 'Quesos', 'Yogurt', 'Mantequilla', 'Crema']
  },
  'panaderia': {
    name: 'Panadería',
    description: 'Pan y productos de panadería frescos',
    image: '/images/categories/panaderia.jpg',
    subcategories: ['Pan', 'Pasteles', 'Galletas', 'Masas', 'Bollería']
  },
  'bebidas': {
    name: 'Bebidas',
    description: 'Amplia variedad de bebidas',
    image: '/images/categories/bebidas.jpg',
    subcategories: ['Agua', 'Jugos', 'Gaseosas', 'Bebidas Calientes', 'Alcohol']
  },
  'despensa': {
    name: 'Despensa',
    description: 'Todo para tu despensa',
    image: '/images/categories/despensa.jpg',
    subcategories: ['Aceites', 'Conservas', 'Pasta', 'Arroz', 'Harinas']
  },
  'congelados': {
    name: 'Congelados',
    description: 'Productos congelados de calidad',
    image: '/images/categories/congelados.jpg',
    subcategories: ['Verduras', 'Carnes', 'Pescados', 'Comidas Preparadas', 'Postres']
  },
  'limpieza': {
    name: 'Limpieza',
    description: 'Productos de limpieza e higiene',
    image: '/images/categories/limpieza.jpg',
    subcategories: ['Detergentes', 'Desinfectantes', 'Papel', 'Limpieza Cocina', 'Accesorios']
  }
} as const;

// Generar rutas estáticas
export function getStaticPaths() {
  const categoriesObj = {
    'frutas-verduras': {
      name: 'Frutas y Verduras',
      description: 'Encuentra la mejor selección de frutas y verduras frescas',
      image: '/images/categories/frutas-verduras.jpg',
      subcategories: ['Frutas', 'Verduras', 'Hierbas y Especias', 'Frutos Secos', 'Orgánicos']
    },
    'carnes-pescados': {
      name: 'Carnes y Pescados',
      description: 'Productos de primera calidad para tu negocio',
      image: '/images/categories/carnes.jpg',
      subcategories: ['Carnes Rojas', 'Pollo', 'Pescados', 'Mariscos', 'Embutidos']
    },
    'lacteos': {
      name: 'Lácteos',
      description: 'Lácteos frescos y de calidad',
      image: '/images/categories/lacteos.jpg',
      subcategories: ['Leche', 'Quesos', 'Yogurt', 'Mantequilla', 'Crema']
    },
    'panaderia': {
      name: 'Panadería',
      description: 'Pan y productos de panadería frescos',
      image: '/images/categories/panaderia.jpg',
      subcategories: ['Pan', 'Pasteles', 'Galletas', 'Masas', 'Bollería']
    },
    'bebidas': {
      name: 'Bebidas',
      description: 'Amplia variedad de bebidas',
      image: '/images/categories/bebidas.jpg',
      subcategories: ['Agua', 'Jugos', 'Gaseosas', 'Bebidas Calientes', 'Alcohol']
    },
    'despensa': {
      name: 'Despensa',
      description: 'Todo para tu despensa',
      image: '/images/categories/despensa.jpg',
      subcategories: ['Aceites', 'Conservas', 'Pasta', 'Arroz', 'Harinas']
    },
    'congelados': {
      name: 'Congelados',
      description: 'Productos congelados de calidad',
      image: '/images/categories/congelados.jpg',
      subcategories: ['Verduras', 'Carnes', 'Pescados', 'Comidas Preparadas', 'Postres']
    },
    'limpieza': {
      name: 'Limpieza',
      description: 'Productos de limpieza e higiene',
      image: '/images/categories/limpieza.jpg',
      subcategories: ['Detergentes', 'Desinfectantes', 'Papel', 'Limpieza Cocina', 'Accesorios']
    }
  };
  
  return Object.keys(categoriesObj).map((slug) => ({
    params: { slug }
  }));
}

const { slug } = Astro.params;

const currentCategory = categories[slug as keyof typeof categories];

// Obtener productos desde la API y filtrar por categoría
const allProducts = await productsApi.getAll({ limit: 100 });
const products = allProducts
  .filter(product => product.category?.toLowerCase() === currentCategory?.name?.toLowerCase())
  .map(product => ({
    id: product.id,
    name: product.name,
    price: product.price,
    discount: 0,
    image_url: product.image_url || '',
    stock: product.stock,
    inStock: product.stock > 0,
    category: product.category,
    brand: product.brand || ''
  }))
  // Ordenar productos: con stock primero, sin stock al final
  .sort((a, b) => {
    if (a.inStock && !b.inStock) return -1;
    if (!a.inStock && b.inStock) return 1;
    return 0;
  });

// Calcular paginación dinámica
const productsPerPage = 100; // Mostrar hasta 100 productos por página
const totalPages = Math.ceil(products.length / productsPerPage);
const currentPage = 1;

// Obtener marcas únicas
const uniqueBrands = [...new Set(products.map(p => p.brand).filter(b => b))].sort();
---

<Layout title={`${currentCategory?.name || 'Categoría'} - Provifood`}>
  <TopBar />
  <Navbar />
  <CategoryMenu />
  
  <main class="bg-gray-50 min-h-screen">
    <!-- Banner de Categoría -->
    <section class="relative h-64 overflow-hidden bg-gradient-to-r from-provifood-primary to-teal-600">
      <div class="absolute inset-0 bg-black/20"></div>
      <div class="container mx-auto px-4 h-full flex items-center relative z-10">
        <div>
          <h1 class="text-4xl font-bold text-white mb-2">{currentCategory?.name}</h1>
          <p class="text-xl text-white/90">{currentCategory?.description}</p>
        </div>
      </div>
    </section>

    <!-- Subcategorías -->
    {currentCategory?.subcategories && (
      <section class="bg-white border-b border-gray-200 py-6">
        <div class="container mx-auto px-4">
          <div class="flex items-center gap-4 overflow-x-auto pb-2">
            <button class="px-6 py-2 bg-provifood-primary text-white rounded-full font-semibold whitespace-nowrap hover:bg-provifood-primary/90">
              Todos
            </button>
            {currentCategory.subcategories.map((sub) => (
              <button class="px-6 py-2 bg-gray-100 text-provifood-dark rounded-full font-semibold whitespace-nowrap hover:bg-gray-200">
                {sub}
              </button>
            ))}
          </div>
        </div>
      </section>
    )}

    <!-- Contenido Principal -->
    <div class="container mx-auto px-4 py-8">
      <div class="flex gap-8">
        <!-- Sidebar de Filtros -->
        <aside class="hidden lg:block w-64 flex-shrink-0">
          <div class="bg-white rounded-lg shadow-sm sticky top-24 max-h-[calc(100vh-6rem)] overflow-y-auto">
            <!-- Marcas -->
            <div class="p-4 border-b border-gray-200">
              <button class="flex items-center justify-between w-full text-left">
                <h3 class="font-bold text-provifood-dark">Marcas</h3>
                <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
              </button>
              <div class="mt-3 space-y-2" id="brands-filter">
                {uniqueBrands.map((brand) => (
                  <label class="flex items-center space-x-2 cursor-pointer">
                    <input type="checkbox" class="rounded text-provifood-primary focus:ring-provifood-primary filter-brand" data-brand={brand} />
                    <span class="text-sm text-provifood-gray">{brand}</span>
                  </label>
                ))}
              </div>
            </div>

            <!-- Precio -->
            <div class="p-4 border-b border-gray-200">
              <button class="flex items-center justify-between w-full text-left">
                <h3 class="font-bold text-provifood-dark">Precio</h3>
                <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
              </button>
              <div class="mt-3 space-y-3">
                <div class="flex items-center gap-2">
                  <input type="number" id="price-min" placeholder="Min" class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm" />
                  <span class="text-gray-400">-</span>
                  <input type="number" id="price-max" placeholder="Max" class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm" />
                </div>
                <button id="apply-price" class="w-full px-4 py-2 bg-provifood-primary text-white rounded-lg text-sm font-semibold hover:bg-provifood-primary/90">
                  Aplicar
                </button>
              </div>
            </div>

            <!-- Disponibilidad -->
            <div class="p-4 border-b border-gray-200">
              <button class="flex items-center justify-between w-full text-left">
                <h3 class="font-bold text-provifood-dark">Disponibilidad</h3>
                <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
              </button>
              <div class="mt-3 space-y-2">
                <label class="flex items-center space-x-2 cursor-pointer">
                  <input type="checkbox" id="filter-in-stock" class="rounded text-provifood-primary focus:ring-provifood-primary" checked />
                  <span class="text-sm text-provifood-gray">En stock</span>
                </label>
                <label class="flex items-center space-x-2 cursor-pointer">
                  <input type="checkbox" id="filter-out-stock" class="rounded text-provifood-primary focus:ring-provifood-primary" />
                  <span class="text-sm text-provifood-gray">Sin stock</span>
                </label>
              </div>
            </div>

            <!-- Botón Aplicar Filtros -->
            <div class="p-4">
              <button id="apply-filters" class="w-full px-4 py-3 bg-provifood-primary text-white rounded-lg font-bold hover:bg-provifood-primary/90 shadow-md">
                Aplicar Filtros
              </button>
            </div>
          </div>
        </aside>

        <!-- Grid de Productos -->
        <div class="flex-1">
          <!-- Barra de ordenamiento -->
          <div class="bg-white rounded-lg shadow-sm p-4 mb-6 flex items-center justify-between">
            <p class="text-provifood-gray">
              <span class="font-semibold text-provifood-dark" id="products-count">{products.length}</span> productos encontrados
            </p>
            <div class="flex items-center gap-3">
              <span class="text-sm text-provifood-gray">Ordenar por:</span>
              <select id="sort-select" class="px-4 py-2 border border-gray-300 rounded-lg text-sm font-semibold text-provifood-dark focus:ring-2 focus:ring-provifood-primary focus:border-transparent">
                <option value="default">Recomendados</option>
                <option value="price-asc">Menor precio</option>
                <option value="price-desc">Mayor precio</option>
                <option value="name">Nombre (A-Z)</option>
              </select>
            </div>
          </div>

          <!-- Grid de productos -->
          <div id="products-grid" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
            {products.map((product) => (
              <div
                class="product-item"
                data-name={product.name}
                data-brand={product.brand}
                data-price={product.price}
                data-in-stock={product.inStock ? 'true' : 'false'}
              >
                <ProductCard product={product} />
              </div>
            ))}
          </div>

          <!-- Paginación -->
          {totalPages > 1 && (
            <div class="mt-8 flex justify-center">
              <nav class="flex items-center gap-2">
                <button class="px-4 py-2 border border-gray-300 rounded-lg text-provifood-dark hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed" disabled={currentPage === 1}>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                  </svg>
                </button>
                {Array.from({ length: totalPages }, (_, i) => i + 1).map((page) => (
                  <button class={`px-4 py-2 border rounded-lg font-semibold ${page === currentPage ? 'bg-provifood-primary text-white border-provifood-primary' : 'border-gray-300 text-provifood-dark hover:bg-gray-50'}`}>
                    {page}
                  </button>
                ))}
                <button class="px-4 py-2 border border-gray-300 rounded-lg text-provifood-dark hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed" disabled={currentPage === totalPages}>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                  </svg>
                </button>
              </nav>
            </div>
          )}
        </div>
      </div>
    </div>
  </main>
  
  <Footer />
</Layout>

<script is:inline>
  // Función para filtrar productos
  function filterProducts() {
    const productItems = document.querySelectorAll('.product-item');
    const selectedBrands = Array.from(document.querySelectorAll('.filter-brand:checked')).map(cb => cb.dataset.brand);
    const minPrice = parseFloat(document.getElementById('price-min').value) || 0;
    const maxPrice = parseFloat(document.getElementById('price-max').value) || Infinity;
    const showInStock = document.getElementById('filter-in-stock').checked;
    const showOutStock = document.getElementById('filter-out-stock').checked;
    
    let visibleCount = 0;
    
    productItems.forEach(item => {
      const brand = item.dataset.brand;
      const price = parseFloat(item.dataset.price);
      const inStock = item.dataset.inStock === 'true';
      
      // Filtrar por marca (si no hay marcas seleccionadas, mostrar todas)
      const brandMatch = selectedBrands.length === 0 || selectedBrands.includes(brand);
      
      // Filtrar por precio
      const priceMatch = price >= minPrice && price <= maxPrice;
      
      // Filtrar por disponibilidad (si ambos o ninguno están marcados, mostrar todos)
      let stockMatch = true;
      if (showInStock && !showOutStock) {
        stockMatch = inStock;
      } else if (!showInStock && showOutStock) {
        stockMatch = !inStock;
      }
      // Si ambos están marcados o ninguno, stockMatch = true (mostrar todos)
      
      if (brandMatch && priceMatch && stockMatch) {
        item.style.display = '';
        visibleCount++;
      } else {
        item.style.display = 'none';
      }
    });
    
    // Actualizar contador
    document.getElementById('products-count').textContent = visibleCount;
  }
  
  // Función para ordenar productos
  function sortProducts(sortBy) {
    const grid = document.getElementById('products-grid');
    const items = Array.from(grid.querySelectorAll('.product-item'));

    items.sort((a, b) => {
      const inStockA = a.dataset.inStock === 'true';
      const inStockB = b.dataset.inStock === 'true';

      // SIEMPRE ordenar por stock primero (con stock antes que sin stock)
      if (inStockA && !inStockB) return -1;
      if (!inStockA && inStockB) return 1;

      // Si ambos tienen el mismo estado de stock, aplicar el ordenamiento seleccionado
      const priceA = parseFloat(a.dataset.price);
      const priceB = parseFloat(b.dataset.price);
      const nameA = a.dataset.name?.toLowerCase() || '';
      const nameB = b.dataset.name?.toLowerCase() || '';

      switch(sortBy) {
        case 'price-asc':
          return priceA - priceB;
        case 'price-desc':
          return priceB - priceA;
        case 'name':
          return nameA.localeCompare(nameB);
        default:
          return 0;
      }
    });

    // Reordenar en el DOM
    items.forEach(item => grid.appendChild(item));
  }
  
  // Event listeners
  function initFilters() {
    // Botón Aplicar Filtros (aplica todos los filtros juntos)
    document.getElementById('apply-filters')?.addEventListener('click', filterProducts);
    
    // Botón aplicar precio (también aplica todos los filtros)
    document.getElementById('apply-price')?.addEventListener('click', filterProducts);
    
    // Ordenamiento (se aplica independientemente)
    document.getElementById('sort-select')?.addEventListener('change', (e) => {
      sortProducts(e.target.value);
    });
  }
  
  document.addEventListener('DOMContentLoaded', initFilters);
  document.addEventListener('astro:page-load', initFilters);
</script>
